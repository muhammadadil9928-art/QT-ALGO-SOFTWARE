name: quotex_algorithm_bot
description: OTC-style trading bot for Quotex with 2-3 candle prediction
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.15.0
  flutter_local_notifications: ^12.0.0
  charts_flutter: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/